# Schemas

This directory contains JSON Schemas for contract artifacts and, later, public payloads.

Initial schemas validate registries and fixture manifests. Domain payload schemas should be added during contract capture.

`request-template.schema.json` validates HTTP or manual capture templates consumed by the fixture capture tool.
