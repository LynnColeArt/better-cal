# Booking Lifecycle Fixtures

Planned fixture payloads for booking reads, writes, idempotency, authorization failures, state transitions, and side effects.

Initial fixture ids are listed in `../../manifests/booking-lifecycle.json`.
