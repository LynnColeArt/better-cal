{
  "$schema": "../schemas/enum-registry.schema.json",
  "status": "draft",
  "description": "Initial enum registry for public and persisted string values.",
  "enums": [
    {
      "id": "enum.fixture.status",
      "status": "accepted",
      "values": ["draft", "accepted", "amended", "security-break", "deprecated", "needs-capture"]
    },
    {
      "id": "enum.fixture.comparisonMode",
      "status": "accepted",
      "values": ["strict", "shape", "schema", "state", "side-effect", "security-break"]
    },
    {
      "id": "enum.auth.mode",
      "status": "draft",
      "values": ["none", "api-key", "platform-client-secret", "platform-access-token", "oauth-access-token", "third-party-access-token", "session", "oauth-client", "public-pkce-client", "public-booking-token"]
    },
    {
      "id": "enum.secret.class",
      "status": "accepted",
      "values": ["public", "input-only", "show-once", "internal-only", "derived"]
    },
    {
      "id": "enum.appInstall.providerExchangeStatus",
      "status": "draft",
      "values": ["queued", "blocked", "credential_ready"]
    },
    {
      "id": "enum.appInstall.providerExchangeMode",
      "status": "draft",
      "values": ["simulated", "provider_oauth"]
    },
    {
      "id": "enum.appInstall.completionMode",
      "status": "draft",
      "values": ["simulated", "provider_oauth"]
    },
    {
      "id": "enum.appInstall.activationMode",
      "status": "draft",
      "values": ["simulated", "provider_oauth"]
    },
    {
      "id": "enum.appInstall.progressStatus",
      "status": "draft",
      "values": ["pending", "external_auth_ready", "handoff_ready", "handoff_consumed", "handoff_expired", "authorization_preview_recorded", "callback_preflight_recorded", "provider_exchange_queued", "provider_exchange_blocked", "provider_credential_ready", "installed", "blocked", "active"]
    },
    {
      "id": "enum.appInstall.nextAction",
      "status": "draft",
      "values": ["start_external_auth", "read_external_auth_descriptor", "preview_authorization", "record_callback_preflight", "await_provider_exchange", "complete_install", "none", "create_new_install_intent", "restart_external_auth"]
    },
    {
      "id": "enum.booking.status",
      "status": "draft",
      "values": ["cancelled", "accepted", "rejected", "pending", "awaiting_host"]
    },
    {
      "id": "enum.membership.role",
      "status": "draft",
      "values": ["MEMBER", "ADMIN", "OWNER"]
    },
    {
      "id": "enum.oauth.clientType",
      "status": "needs-capture",
      "values": []
    },
    {
      "id": "enum.oauth.clientStatus",
      "status": "needs-capture",
      "values": []
    },
    {
      "id": "enum.webhook.triggerEvent",
      "status": "draft",
      "values": ["BOOKING_RESCHEDULED", "BOOKING_CANCELLED", "BOOKING_CONFIRMED", "BOOKING_REJECTED"]
    },
    {
      "id": "enum.location.type",
      "status": "needs-capture",
      "values": []
    },
    {
      "id": "enum.payment.status",
      "status": "needs-capture",
      "values": []
    }
  ]
}
